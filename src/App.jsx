// App.jsx — public fan view entry point — v2
// No login required. FanApp handles its own optional auth for subscriptions.
import FanApp from './FanApp'
export default function App() {
  return <FanApp />
}